import React from 'react';

const validate = (props) => {
    return (
        <div>
            <p>
                Text length: {props.length}
            </p>
        </div>
    )
}

export default validate;